# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

- **プロジェクト名**: task-board
- **概要**: タスクを「未着手 / 進行中 / 完了」などのステータスで管理するタスクボード（カンバン）Web アプリケーション。
- **技術スタック**: HTML / CSS / JavaScript（バニラJS、フレームワーク不使用）
- **現状**: プロジェクトはまだ空の状態。これから実装を進める。

## 開発方針

- 外部ライブラリやフレームワークは原則使用せず、素の HTML / CSS / JavaScript で実装する。
- ビルドツールは不要。ブラウザで `index.html` を直接開けば動作すること。
- モダンブラウザ（Chrome / Safari / Firefox / Edge の最新版）で動作すること。
- タスクデータの永続化が必要な場合は、まず `localStorage` の利用を検討する（サーバー不要で完結させる）。

## ディレクトリ構成（想定）

```
task-board/
├── index.html        # エントリーポイント（ボードの画面マークアップ）
├── css/
│   └── style.css     # スタイル定義（カラム・カードのレイアウト等）
└── js/
    └── app.js        # タスクの追加・移動・削除・保存などのロジック
```

## コーディング規約

- **HTML**: セマンティックなタグを使用する。アクセシビリティ（label, alt, aria 属性など）に配慮する。
- **CSS**: クラス名は分かりやすい命名にする。レスポンシブ対応（スマホ・PC両対応）を意識する。
- **JavaScript**:
  - `const` / `let` を使用し、`var` は使わない。
  - 変数名・関数名は英語のキャメルケース、コメントは日本語で記述する。
  - 機能ごとに関数を分割し、可読性を保つ。

## 実行・確認方法

ブラウザで `index.html` を開いて動作を確認する。

```bash
# macOS の場合
open index.html
```

## Git 運用ルール

- **コードを変更するたびに GitHub にプッシュすること。** 動作確認まで終えた変更は、その都度コミットしてリモート（GitHub）へ push する。
- 1 コミットには意味のある単位の変更をまとめる（大きな変更を溜め込まない）。
- コミットメッセージは日本語で、何を変更したかが分かるように書く（例: `タスクカードのドラッグ移動機能を追加`）。
- 基本的な手順:

  ```bash
  git add -A
  git commit -m "変更内容を説明するメッセージ"
  git push origin main
  ```

- まだ Git リポジトリになっていない場合は、初回のみ以下を実行してから運用を始める:

  ```bash
  git init
  git remote add origin <GitHubリポジトリのURL>
  ```

## コミュニケーション

- **返答は必ず日本語で行うこと。**
- コード内のコメントも日本語で記述する。
